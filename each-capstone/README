— Install Backend Dependencies:
cd backend 
pip install -r requirements.txt

— Run Backend Server:
python -m uvicorn app:app --reload --port 8003

— Setup Frontend:
npm install

— Run Frontend:
npm run dev
